[build]
  command = "npm run build"
  publish = "build"

# Redirect all requests to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Set environment variables
[build.environment]
  CI = ""
  NODE_VERSION = "18"
